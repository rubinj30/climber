<div class="path">
    <a lass='path-color' href="/users">Home</a> / {{user.username}}
</div>

<div class='climber-info-container'>
    <div>{{user.username}}</div>
    <div>E-mail: {{user.email}}</div>
    <div>Age: {{user.age}}</div>
    <div>Lives in: {{user.livesIn}}</div>
    <div>Likes to Climb: {{user.likesToClimb}}</div>
</div>
<div class='profile-photo-container'>
    <img src="{{user.photo}}" alt="Climber's Profile Photo">
</div>

<button class="btn waves-effect waves-light button-colors">
    <a class="button-colors" href="/users/{{user._id}}/edit">Edit Climber Info</a>
</button>

<div>
    <a class='button-colors' href="/users/{{user._id}}/delete/">
        <button class="btn waves-effect waves-light button-colors">
            Delete Climber profile
        </button>
    </a>
</div>